# CS101 2026

大一程式設計課程的 C 語言練習與作業。

## 在 VS Code 使用

1. 開啟一個 `.c` 檔案。
2. 按 `Ctrl+Shift+B` 編譯；執行檔會放在 `build` 資料夾。
3. 按 `F5` 編譯並使用 GDB 除錯。

專案使用 C17、GCC 與 GDB。範例程式位於 `src/hello.c`。

## GitHub 日常流程

```powershell
git pull
git add .
git commit -m "描述這次完成的內容"
git push
```
